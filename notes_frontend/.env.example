# Environment variables for the Notes Frontend
# Set the base URL for REST API (backend). If running frontend and backend together, this can be /api or http://localhost:5000/api, etc.
REACT_APP_API_BASE_URL=http://localhost:5000/api
